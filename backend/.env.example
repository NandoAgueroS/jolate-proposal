# JOLATE 2026 — Backend environment variables
# Copy to .env and fill in real values.
# .env is gitignored — never commit real credentials.

SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_ENCRYPTION=
SMTP_FROM_EMAIL=
SMTP_FROM_NAME=
SMTP_COMMITTEE_EMAILS=
